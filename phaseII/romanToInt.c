int romanToInt(char * s);

/**
 * main - Entry point.
 *
 * Return: 0 always on sucess
*/
int main(void)
{
    romanToInt(III);

    return (0);
}

int romanToInt(char * s){
    /*check the values*/
    int i = 0;

    while (s[i])
    {
        i++;
    }
}